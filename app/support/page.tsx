"use client";
import { FormEvent, useState } from "react";
import { Badge, PageShell } from "../ui";
export default function Page(){const [status,setStatus]=useState<"idle"|"sending"|"done"|"error">("idle");const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus("sending");setError("");const form=new FormData(e.currentTarget);const res=await fetch("/api/support",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});if(!res.ok){const data=await res.json() as {error?:string};setError(data.error??"Unable to send your request.");setStatus("error");return}setStatus("done")}return <PageShell><section className="page-hero"><div className="shell"><Badge>Customer support</Badge><h1>How can we help?</h1><p>Send an account, billing, privacy or product request. Do not upload confidential compliance documents through this form.</p></div></section><div className="content-shell support-grid"><section className="panel panel-body"><h2>Submit a request</h2>{status==="done"?<div className="support-success" role="status"><b>Request received</b><p>Your reference has been saved. The support team will respond using the email you provided.</p></div>:<form onSubmit={submit}><div className="field"><label htmlFor="support-name">Name</label><input id="support-name" name="name" required maxLength={100}/></div><div className="field"><label htmlFor="support-email">Email</label><input id="support-email" name="email" type="email" required maxLength={200}/></div><div className="field"><label htmlFor="support-category">Category</label><select id="support-category" name="category"><option>Account access</option><option>Billing or cancellation</option><option>Privacy or data request</option><option>Product question</option><option>Technical issue</option></select></div><div className="field"><label htmlFor="support-message">Message</label><textarea id="support-message" name="message" required minLength={10} maxLength={3000}/></div><label className="support-consent"><input type="checkbox" name="consent" value="yes" required/> I agree that my details may be used to respond to this request.</label>{error&&<p className="form-error" role="alert">{error}</p>}<button className="button primary" disabled={status==="sending"}>{status==="sending"?"Sending…":"Send request"}</button></form>}</section><aside className="panel info-card"><h2>Response scope</h2><p>Support can help with account access, software features, billing records, cancellations, refunds and data-rights requests.</p><p>Support does not provide legal advice or make binding regulatory classifications.</p><h2>Billing support</h2><p>Once live payments are enabled, purchase receipts and subscription-management links will identify the Merchant of Record and its buyer-support route.</p></aside></div></PageShell>}
