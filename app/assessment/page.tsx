"use client";
import { useState } from "react";
import { PageShell, PageVisual } from "../ui";
const qs=['Is the item necessary to contain or support the product throughout its lifetime?','Is it intended to be used for long-term storage?','Is it normally discarded after purchase?','Does it have an independent function?','Is it sold as part of the product?','Is it designed for repeated use?','Would the consumer normally keep it for the lifetime of the product?'];
export default function Page(){const [answers,setAnswers]=useState<Record<number,string>>({});const complete=Object.keys(answers).length===qs.length;const durable=['yes','yes','no','yes','yes','yes','yes'].filter((x,i)=>answers[i]===x).length>=5;return <PageShell><section className="page-hero"><div className="shell"><div className="eyebrow"><i/> Classification support</div><h1>Is it a product or packaging?</h1><p>Explore how function, intended life and consumer use can affect the assessment—without an automatic legal classification.</p></div></section><PageVisual label="FUNCTION-FIRST CHECK" title="Assess the item’s real purpose before classifying it."/><div className="content-shell tool-layout"><section className="panel"><div className="panel-head"><h2>Product vs packaging assessment</h2><span className="muted-note">{Object.keys(answers).length} / {qs.length}</span></div><div className="panel-body">{qs.map((q,i)=><div className="field" key={q}><label>{q}</label><div className="option-grid">{['yes','no','unsure'].map(a=><label className={`option ${answers[i]===a?'selected':''}`} key={a}><input type="radio" name={`q${i}`} onChange={()=>setAnswers({...answers,[i]:a})}/>{a[0].toUpperCase()+a.slice(1)}</label>)}</div></div>)}</div></section><aside><div className="panel info-card sticky"><h3>Assessment</h3>{!complete?<p>Answer all seven questions to see an indicative result.</p>:<><span className={`tag ${durable?'review':'verify'}`}>{durable?'Potentially part of the product':'Requires individual assessment'}</span><p>A durable wooden jewellery case designed for long-term storage may require a different assessment from disposable presentation packaging. Classification depends on its actual function and design. External shipping materials may still constitute packaging.</p></>}<div className="notice">This assessment is informational and is not a binding legal classification.</div></div></aside></div></PageShell>}
